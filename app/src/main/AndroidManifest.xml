<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Phone permission for phone authentication -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- Contact permission for syncing contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Location permissions for maps -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Respondr">
        
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCjVsj3flP-sqYVTSC68PB8aL_oP04uF-I" />
        
        <!-- Landing Activity -->
        <activity
            android:name=".LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
        
        <!-- Sign In Activity -->
        <activity
            android:name=".SignInActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
        
        <!-- Sign Up Activity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
        
        <!-- Onboarding Activity -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
        
        <!-- Contact Permission Activity -->
        <activity
            android:name=".ContactPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
        
        <!-- Auth Activity (Legacy) -->
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.Respondr" />
            
    </application>

</manifest>